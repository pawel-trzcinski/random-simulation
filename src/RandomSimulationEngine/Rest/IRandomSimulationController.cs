using System;

namespace RandomSimulationEngine.Rest
{
    /// <summary>
    /// Default RandomSimulation controller interface. Created for the purpose of <see cref="SimpleInjector"/>'s usage.
    /// </summary>
    public interface IRandomSimulationController : IDisposable
    {
    }
}
